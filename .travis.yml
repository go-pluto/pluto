language: go

go:
  - tip

before_install:
  - make deps
  - make test-pki
  - make test-public

script:
  - make build
  - make test

after_success:
  - bash <(curl -s https://codecov.io/bash) || echo "Codecov did not collect coverage reports"

notifications:
  email:
    on_success: change
    on_failure: always